# Export rom name
export VENDOR := jedi
export VENDOR_LONG := JEDI OPEN SOURCE PROJECT

# Version info
ROM_VERSION_MAJOR := 1
ROM_VERSION_MINOR := 0
ROM_VERSION_MAINTENANCE := .$(shell date -u +%m%d%y)
